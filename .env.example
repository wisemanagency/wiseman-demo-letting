# Sanity
SANITY_PROJECT_ID=your_project_id
SANITY_DATASET=production
SANITY_API_VERSION=2024-01-01
SANITY_TOKEN=your_read_token

# One-off migration scripts (scripts/fix-portabletext-marks.mjs). Must be
# write-capable. Generate at https://www.sanity.io/manage → API → Tokens.
SANITY_MIGRATION_TOKEN=your_write_token

# Optional: Form endpoint (Formspree, Netlify Forms, etc.)
PUBLIC_FORM_ENDPOINT=https://formspree.io/f/your_form_id
