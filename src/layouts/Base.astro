---
import SEOHead from "@/components/SEOHead.astro";
import { sanityClient } from "@/lib/sanity";
import { siteSettingsQuery } from "@/lib/queries";
import "@/styles/global.css";

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  jsonLd?: Record<string, unknown>;
  noindex?: boolean;
}

const settings = await sanityClient.fetch(siteSettingsQuery);
const {
  title = settings?.defaultMetaTitle || "Estate Agent Demo",
  description = settings?.defaultMetaDescription,
  ogImage = settings?.ogImage?.asset?.url,
  jsonLd,
  noindex,
} = Astro.props;

const siteName = settings?.siteName || "Estate Agent Demo";
const fullTitle = title.includes(siteName) ? title : `${title} | ${siteName}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <SEOHead
      title={fullTitle}
      description={description}
      ogImage={ogImage}
      jsonLd={jsonLd}
      noindex={noindex}
    />
    {settings?.favicon?.asset?.url && (
      <link rel="icon" type="image/png" href={settings.favicon.asset.url} />
    )}
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    {settings?.googleAnalyticsId && (
      <script
        is:inline
        async
        src={`https://www.googletagmanager.com/gtag/js?id=${settings.googleAnalyticsId}`}
      />
    )}
  </head>
  <body class="min-h-screen flex flex-col">
    <!-- Header -->
    <header class="site-header">
      <div class="container flex items-center justify-between">
        <a href="/" class="wordmark">
          {siteName}
        </a>

        <nav class="site-nav hidden md:flex">
          <a href="/properties" class="nav-link">Properties</a>
          <a href="/map" class="nav-link">Map Search</a>
          <a href="/areas" class="nav-link">Areas</a>
          <a href="/team" class="nav-link">Our Team</a>
          <a href="/about" class="nav-link">About</a>
          <a href="/contact" class="nav-link">Contact</a>
          <a href="/valuation" class="btn btn-primary nav-cta">Book Valuation</a>
        </nav>

        <!-- Mobile menu button -->
        <button
          class="md:hidden p-2 text-white"
          aria-label="Open menu"
          onclick="document.getElementById('mobile-menu')?.classList.toggle('hidden')"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>

      <!-- Mobile menu -->
      <div id="mobile-menu" class="hidden md:hidden site-mobile-menu">
        <nav>
          <a href="/properties">Properties</a>
          <a href="/map">Map Search</a>
          <a href="/areas">Areas</a>
          <a href="/team">Our Team</a>
          <a href="/about">About</a>
          <a href="/contact">Contact</a>
          <a href="/valuation" class="btn btn-primary" style="margin: 12px 20px;">Book Valuation</a>
        </nav>
      </div>
    </header>

    <!-- Main -->
    <main class="flex-1">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="site-footer mt-auto">
      <div class="container py-12 md:py-16">
        <div class="grid md:grid-cols-3 gap-10">
          <div>
            <h4>{siteName}</h4>
            {settings?.phone && (
              <div class="footer-link">
                <a href={`tel:${settings.phone}`}>{settings.phone}</a>
              </div>
            )}
            {settings?.email && (
              <div class="footer-link">
                <a href={`mailto:${settings.email}`}>{settings.email}</a>
              </div>
            )}
          </div>
          <div>
            <h4>Quick Links</h4>
            <nav class="flex flex-col">
              <div class="footer-link"><a href="/properties">All Properties</a></div>
              <div class="footer-link"><a href="/areas">Areas We Cover</a></div>
              <div class="footer-link"><a href="/valuation">Free Valuation</a></div>
              <div class="footer-link"><a href="/contact">Contact Us</a></div>
            </nav>
          </div>
          <div>
            {settings?.socialLinks?.length > 0 && (
              <>
                <h4>Follow Us</h4>
                <div class="flex gap-4">
                  {settings.socialLinks.map((link: { platform: string; url: string }) => (
                    <a
                      href={link.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-sm"
                    >
                      {link.platform}
                    </a>
                  ))}
                </div>
              </>
            )}
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <div class="container">
          <div class="wordmark">{siteName}</div>
          {settings?.footerText || `© ${new Date().getFullYear()} ${siteName}. All rights reserved.`}
        </div>
      </div>
    </footer>
  </body>
</html>
